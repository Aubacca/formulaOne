/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MashitPipe } from './mashit.pipe';

describe('Pipe: Mashit', () => {
  it('create an instance', () => {
    let pipe = new MashitPipe();
    expect(pipe).toBeTruthy();
  });
});
