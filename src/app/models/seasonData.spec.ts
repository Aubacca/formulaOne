/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import { SeasonData } from './seasonData';

describe('SeasonData', () => {
  it('should create an instance', () => {
    expect(new SeasonData()).toBeTruthy();
  });
});
