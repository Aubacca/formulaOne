/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import { DriverData } from './driverData';

describe('DriverData', () => {
  it('should create an instance', () => {
    expect(new DriverData()).toBeTruthy();
  });
});
