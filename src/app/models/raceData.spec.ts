/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import { RaceData } from './raceData';

describe('RaceData', () => {
  it('should create an instance', () => {
    expect(new RaceData()).toBeTruthy();
  });
});
